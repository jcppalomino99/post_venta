PUBLIC_DIRECTORY_PASSWORD=change_me_in_production

# SQLite y uploads: en local se usan data/postventa.sqlite y carpeta uploads/.
# En Render con disco persistente montado en /var/data (ver render.yaml):
# DB_PATH=/var/data/postventa.sqlite
# UPLOADS_DIR=/var/data/uploads
